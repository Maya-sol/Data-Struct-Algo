#include <iostream>


int main()
{
    unsigned long long int Number, Reverse=0;
    std::cin>> Number;
    while(Number > 0)
    {
        Reverse = (Reverse * 10) + Number % 10;
        Number /= 10;
    }
    std::cout<< Reverse;

    return 0;
}

